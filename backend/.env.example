# DebateOS Environment Configuration
LLM_PROVIDER=gemini
GEMINI_MODEL=gemini-2.0-flash

# Optional: keep OpenAI credentials empty unless needed
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

MAX_ROUNDS=3
EARLY_STOP_CONFIDENCE=85
